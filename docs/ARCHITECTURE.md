# Architecture

## System Overview

Mo Headlines uses a **workflow-based architecture** separating content creation from content review. The system processes articles from multiple sources (RSS, manual research, YouTube) through a unified pipeline ending in an editorial review process.

### Core Workflow
```
Create Sources → Unified Queue → AI Processing → Review → Publish
```

## Database Schema

### Core Content Tables

```typescript
articles: {
  _id: Id<"articles">
  title: string
  body: string
  topics: array
  catergoyId: Id<"catergories">
  imageStorageId?: Id<"_storage">
  imageGenPrompts: string
  excerpt?: string
  status: "approved" | "rejected" | "draft" | "pending"
  isAutoGenerated: boolean
  isEdited: boolean
  sourceUrls: string[]
  sourceType: "rss" | "research" | "youtube"
  sourceId: Id<"create_rss"> | Id<"create_research"> | Id<"create_youtube">
  viewCount: number
  publishedAt?: number
  updatedAt: number
  authorId?: Id<"users">
  slug: string
}

categories: {
  _id: Id<"categories">
  name: string
  slug: string
  keywords: string[]
  isActive: boolean
  updatedAt?: number
}

comments: {
  _id: Id<"comments">
  articleId: Id<"articles">
  userId: string
  username: string
  avatarUrl: string
  content: string
  updatedAt?: number
  isApproved: boolean
}

users: {
  _id: Id<"users">
  clerkId: string
  username?: string
  updatedAt?: number
}
```

### Create Workflow Tables

```typescript
create_rss: {
  _id: Id<"create_rss">
  name: string
  url: string
  categoryId: Id<"categories">
  status: "pending" | "processing" | "complete"
  isActive: boolean
  pollFrequency: number
  numberOfArticles: number
  lastPolled?: number
  nextRunTime?: number
  updatedAt: number
  createdBy: Id<"users">
  
  // Run result fields
  lastRunSuccess?: boolean
  lastRunFeedStatus?: string
  lastRunCategoryStatus?: string
  lastRunArticlesFound?: number
  lastRunArticlesQueued?: number
  lastRunArticles?: Array<{
    title: string
    url: string
    publishedAt: string
    excerpt: string
  }>
  lastRunError?: string
}
// Indexes
// - by_category: ["categoryId"]
// - by_active: ["isActive"] 
// - by_next_run: ["nextRunTime"]

create_research: {
  _id: Id<"create_research">
  title: string
  researchQuery: string
  status: "pending" | "processing" | "complete"
  createdBy: Id<"users">
  updatedAt?: number
}

create_youtube: {
  _id: Id<"create_youtube">
  videoUrl: string
  videoTitle?: string
  concept: string
  status: "pending" | "processing" | "complete"
  duration?: number
  category: string
  timecodeStart?: number
  timecodeEnd?: number
  createdBy: Id<"users">
  updatedAt?: number
}

create_queue: {
  _id: Id<"create_queue">
  sourceType: "rss" | "research" | "youtube"
  sourceId: Id<"create_rss"> | Id<"create_research"> | Id<"create_youtube">
  status: "pending" | "processing" | "complete"
  title: string
  category: string
  priority: number
  processingInstructions: string
  queuedAt: number
  processedAt?: number
  isProcessing: boolean
  processed: boolean
}
```

## Data Flow Architecture

### Create Workflow
```
RSS Producer → Create Queue ↘
Research Article → Create Queue → AI Processing → Articles (status: pending)
YouTube Video → Create Queue ↗
```

### Review Workflow
```
Articles (status: pending) → Review Interface → Admin Decision → Articles (status: approved/rejected/draft)
```

### Publishing Flow
```
Articles (status: approved) → Public Website
```

## Component Architecture

### Shared Services

#### 1. Queue Management Service
**Location**: `/components/admin/shared/queue/`
- **Purpose**: Manages create_queue operations across all source types
- **Components**: QueueCard, QueueStats, QueueProcessor
- **Used by**: RSS tab, Research tab, YouTube tab

#### 2. Tab System Service
**Location**: `/components/admin/shared/tabs/`
- **Purpose**: Consistent tab navigation for Create and Review sections
- **Components**: TabContainer, TabButton, TabContent
- **Used by**: Create section (RSS/Research/YouTube), Review section (Pending/Approved/Rejected)

#### 3. Universal Create Card
**Location**: `/components/admin/shared/cards/`
- **Purpose**: Displays source information consistently across all create types
- **Components**: CreateCard, StatusIndicator, ActionMenu
- **Variants**: RssCard, ResearchCard, YouTubeCard (extend base CreateCard)

#### 4. Article Status Management
**Location**: `/components/admin/shared/status/`
- **Purpose**: Handles article status transitions and display
- **Components**: StatusBadge, StatusActions, StatusHistory
- **Used by**: Review workflow, article management

### Component Boundaries

```
/components/admin/
├── create/
│   ├── rss/
│   │   ├── RssTab.tsx
│   │   ├── RssSourceForm.tsx
│   │   └── RssSourceCard.tsx (extends CreateCard)
│   ├── research/
│   │   ├── ResearchTab.tsx
│   │   ├── ResearchForm.tsx
│   │   └── ResearchCard.tsx (extends CreateCard)
│   ├── youtube/
│   │   ├── YouTubeTab.tsx
│   │   ├── YouTubeForm.tsx
│   │   └── YouTubeCard.tsx (extends CreateCard)
│   └── CreateLayout.tsx (tab container)
├── review/
│   ├── pending/
│   │   ├── PendingTab.tsx
│   │   └── PendingArticleCard.tsx
│   ├── approved/
│   │   └── ApprovedTab.tsx
│   ├── rejected/
│   │   └── RejectedTab.tsx
│   ├── drafts/
│   │   └── DraftsTab.tsx
│   └── ReviewLayout.tsx (tab container)
└── shared/
    ├── queue/
    ├── tabs/
    ├── cards/
    └── status/
```

## AI Processing Pipeline

### Queue Processing Strategy
- Individual Processing (Phase 1)
- Batch Processing (Phase 2)

### Prompt Engineering Strategy
- **Source-specific instructions**: Different prompts for RSS vs Research vs YouTube
- **Category context**: Include category keywords for relevant fact-checking
- **Quality markers**: Specify expected output format and quality criteria

## Integration Points

### Create → Queue
- All create sources add items to unified `create_queue`
- Source type determines processing instructions
- Priority system for urgent vs routine processing

### Queue → Processing
- Configurable: individual vs batch processing
- Error handling and retry logic
- Processing status tracking

### Processing → Review
- Articles created with `status: "pending"`
- Automatic population of Review/Pending section
- Metadata preserved for source traceability

### Review → Publish
- Status change from "pending" to "approved"
- Automatic appearance on public website
- SEO optimization and social sharing preparation

## Performance Considerations

### Database Optimization
- Indexes on `articles.status` for Review workflow queries
- Indexes on `create_queue.processed` and `create_queue.sourceType`
- Efficient category keyword matching

### Queue Processing
- Configurable batch sizes based on API limits
- Rate limiting compliance with Perplexity API
- Background processing with status updates

## Security Architecture

### Authentication
- Clerk integration for admin access
- Role-based permissions for Create vs Review workflows
- API key management for external services

### Data Validation
- Zod schemas for all form inputs
- URL validation for RSS and YouTube sources
- Content sanitization before storage

### API Security
- Rate limiting on admin endpoints
- Input validation and sanitization
- Secure handling of external API keys

## Scalability Considerations

### Future Source Types
- Plugin architecture for new create types (Twitter, Podcasts, Conferences)
- Standardized queue interface for any source type
- Extensible prompt system for new processing requirements

### Performance Scaling
- Queue processing can be moved to background workers
- Database sharding by category or time period
- CDN integration for published articles

### Monitoring & Analytics
- Queue processing metrics
- Article engagement tracking
- Source performance monitoring